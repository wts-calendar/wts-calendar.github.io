name: Test and deploy calendar portal

on:
  push:
    branches: [wts/source]
  pull_request:
    branches: [wts/source]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22.22.3'
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Check showcase contracts
        run: npm run check:release
      - name: Test calendar and routing
        run: npm test -- --watch=false
      - name: Build and verify prerendered SEO pages
        run: npm run build:pages:org
      - name: Upload static website
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/wts/source'
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: dist/wts-calendar-angular-example/browser

  deploy:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/wts/source'
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
